package co.kernelnetworks.medstream.server.models.shared

abstract class UserBase(firstName: String,
                        lastName: String,
                        email: String,
                        company: Option[String])
